/* ⭐ Program to add two numbers ⭐ */


#include <stdio.h>

int main() {
    int a, b;

    // Input two numbers
    printf("Enter two numbers: ");
    scanf("%d %d", &a, &b);

    // Output their sum directly
    printf("Sum = %d\n", a + b);

    return 0;
}
